#%Module1.0#####################################################################
##
## trim_galore modulefile
##

source $env(MODULE_INCLUDE)/functions.tcl
getCluster

set components [ file split [ module-info name ] ]
set version [ lindex $components 1 ]

set     modroot          /proj/a2010002/nobackup/sw/mf/bioinfo-tools/pipelines/trim_galore/$version

proc ModulesHelp { } {
        global version modroot

        puts stderr "\ttrim_galore - use Trim Galore! $version"
        puts stderr "\n\tVersion $version\n"
}

module-whatis   "Loads Trim Galore! environment."

# Only one version at a time
conflict trim_galore

#Log loading to syslog
logToSyslog

if [module-info mode load] {
    prepend-path        PATH            $modroot
}

if [module-info mode remove] {
    remove-path         PATH            $modroot
}
